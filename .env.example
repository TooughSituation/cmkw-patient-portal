# Auth.js / NextAuth v5 — wymagane do podpisu JWT
# Wygeneruj: openssl rand -base64 32
AUTH_SECRET=replace-with-random-secret

# Opcjonalnie (domyślnie trustHost: true)
# AUTH_URL=http://localhost:3000
